apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ template "github-actions-runner.fullname" . }}"
  labels:
    app.kubernetes.io/name: {{ include "github-actions-runner.name" . | quote }}
    helm.sh/chart: {{ include "github-actions-runner.chart" . | quote }}
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount | quote }}
{{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name | quote }}
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name | quote }}
        app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    spec:
      volumes:
      - name: dockersocket
        hostPath:
          path: "/var/run/docker.sock"
      containers:
      - name: runner
        image: {{ printf "%s:%s"  .Values.image.repository .Values.image.tag | quote }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
        env:
        {{- if .Values.env.org.runner }}
        - name: ORG_RUNNER
          value: "true"
        - name: ACCESS_TOKEN
          valueFrom:
            secretKeyRef:
              name: {{ .Values.env.org.secret.access_token.name | quote }}
              key: "token"
        - name: ORG_NAME
          value: {{ .Values.env.org.name | quote }}{{ else }}
        - name: REPO_URL
          value: {{ .Values.env.repo_url | quote }}
        - name: ORG_RUNNER
          value: "false"
        {{- end }}
        - name: LABELS
          value: {{ .Values.env.labels | quote }}
        {{- if .Values.env.name_prefix }}
        - name: RUNNER_NAME_PREFIX
          value: {{ .Values.env.name_prefix | quote }}
        {{- else }}
        - name: RUNNER_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        {{- end }}
        volumeMounts:
        - name: dockersocket
          mountPath: "/var/run/docker.sock"
